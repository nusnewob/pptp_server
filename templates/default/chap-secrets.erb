# Secrets for authentication using CHAP
# client        server  secret                  IP addresses

<% node[:pptpd][:users].each do |accout| %>
<%= accout[:username] %> pptpd <%= accout[:password] %> *
<% end %>
